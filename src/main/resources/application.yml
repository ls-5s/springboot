spring:
  application:
    name: demo3
  profiles:
    active: dev
  messages:
    encoding: UTF-8
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

# JWT 配置
jwt:
  secret: your-256-bit-secret-key-for-blog-project
  expire: 604800
